import { FilterOriginalRequestsPipe } from './filter-original-requests.pipe';

describe('FilterOriginalRequestsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOriginalRequestsPipe();
    expect(pipe).toBeTruthy();
  });
});
